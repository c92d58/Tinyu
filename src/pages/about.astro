---
import Layout from "../components/Layout.astro";
import Heading from "../components/Heading.astro";
---

<Layout
  title="WAHSUNの自留地"
  description="閱讀、思考與生活的筆記。一個人在試圖穿透自己頭腦中的迷霧時，留下的文字。"
>
  <section class="about">

    <!-- ─── Hero ─────────────────────────────── -->
    <header class="about-hero">
      <Heading as="h1">
        <span class="hero-heading-main">WAHSUNの自留地</span>
      </Heading>
        一個人在試圖穿透自己頭腦中的迷霧時，留下的筆記。
      </p>
    </header>

    <!-- ─── Bio ──────────────────────────────── -->
    <div class="about-card card animate-fade-up delay-2">
      <div class="about-card-content">

        <p class="manifesto-lead">
          人類最大的錯覺，是以為「一無所知」會讓人感到膽怯。一無所知只會讓人無所畏懼。
        </p>

        <p>
          這個網站存在的理由，就是跟自己過不去。它不是為了說服誰，也不是為了傳遞某種「真理」——恰恰相反，它始於一個簡單的懷疑：<strong>「我以為我知道的東西，有多少是真的？」</strong>
        </p>

        <p>
          於是我開始寫。寫那些讓我輾轉反側的問題，寫那些在閱讀中突然擊中我的句子，寫那些在日常生活中發現的荒謬與美好。這個過程沒有終點，也不需要終點——<strong>思考本身就是目的</strong>。
        </p>

        <blockquote class="manifesto-quote">
          <p>「知識的盡頭不是答案，而是更深的問題。」</p>
        </blockquote>

        <h3>我在寫什麼</h3>

        <ul class="manifesto-list">
          <li><strong>閱讀筆記</strong> — 書中的觀點如何與現實交織</li>
          <li><strong>思想隨筆</strong> — 對社會、心理、系統的觀察與反思</li>
          <li><strong>生活記錄</strong> — 日常中那些值得被記住的片刻</li>
        </ul>

        <h3>關於作者</h3>

        <p>
          只是一個對世界充滿好奇的人。相信獨立思考的價值，相信文字的力量，也相信——在一片喧囂中，仍然有人愿意坐下來，好好讀完一篇文章。
        </p>

        <p class="manifesto-tagline">
          如果你也在尋找那些未被演算法過濾的真實，那麼我們可能成為朋友。
        </p>

      </div>
    </div>

    <!-- ─── Contact ──────────────────────────── -->
    <div class="about-card card animate-fade-up delay-4">
      <div class="contact-inner">
        <div class="contact-row">
          <span class="contact-label">電郵</span>
          <a href="mailto:x@wahsun.org" class="contact-value">x@wahsun.org</a>
        </div>
        <div class="contact-row">
          <span class="contact-label">站點</span>
          <span class="contact-value">blog.wahsun.org</span>
        </div>
      </div>
    </div>

  </section>
</Layout>

<style>
  .about { padding: 2rem 0 4rem; }

  /* ─── Hero ──────────────────────────────── */
  .about-hero {
    text-align: center;
    padding: 2rem 0 2.5rem;
  }
  .hero-heading-main {
    font-family: var(--font-heading);
    font-size: 2.25rem;
    font-weight: 800;
    background: var(--gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* ─── About Card ────────────────────────── */
  .about-card {
    margin-bottom: 1.5rem;
    padding: 2rem;
  }
  .about-card-content {
    max-width: 580px;
    margin: 0 auto;
  }
  .about-card h3 {
    font-family: var(--font-heading);
    font-size: 1.125rem;
    font-weight: 700;
    margin: 2rem 0 0.75rem;
    position: relative;
    padding-left: 0.75rem;
  }
  .about-card h3::before {
    content: '';
    position: absolute;
    left: 0; top: 0.15rem; bottom: 0.15rem;
    width: 3px;
    background: var(--gradient);
    border-radius: 2px;
  }
  .about-card p {
    font-size: 0.9375rem;
    line-height: 1.75;
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }
  .about-card strong {
    color: var(--text);
    font-weight: 600;
  }

  .manifesto-lead {
    font-size: 1.125rem !important;
    font-style: italic;
    color: var(--text) !important;
    font-weight: 500;
    padding: 1rem 1.5rem;
    margin-bottom: 1.5rem !important;
    border-left: 3px solid var(--accent);
    background: var(--accent-subtle);
    border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  }

  .manifesto-quote {
    margin: 1.5rem 0;
    padding: 1rem 1.5rem;
    border-left: 3px solid var(--border);
    font-style: italic;
    color: var(--text-secondary);
  }
  .manifesto-quote p {
    margin-bottom: 0 !important;
  }

  .manifesto-list {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .manifesto-list li {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    padding: 0.5rem 0.75rem;
    border-radius: var(--radius-sm);
    background: var(--accent-subtle);
  }
  .manifesto-list li strong {
    color: var(--text);
  }

  .manifesto-tagline {
    font-size: 1rem !important;
    font-style: italic;
    color: var(--accent) !important;
    text-align: center;
    margin-top: 2rem !important;
    padding: 1rem;
    border: 1px solid var(--accent-subtle);
    border-radius: var(--radius-md);
    background: var(--accent-subtle);
  }

  /* ─── Contact ──────────────────────────── */
  .contact-inner {
    max-width: 400px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .contact-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 0;
    border-bottom: 1px solid var(--border-light);
  }
  .contact-row:last-child { border-bottom: none; }
  .contact-label {
    font-family: var(--font-heading);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-tertiary);
  }
  .contact-value {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s;
  }
  .contact-value:hover {
    color: var(--accent);
  }

  /* ─── Responsive ────────────────────────── */
  @media (max-width: 600px) {
    .about-hero { padding: 1.5rem 0; }
    .hero-heading-main { font-size: 1.75rem; }
    .about-card { padding: 1.25rem; }
    .manifesto-lead { font-size: 1rem !important; padding: 0.75rem 1rem; }
  }
</style>
