---
interface Props {
  as?: "h1" | "h2" | "h3";
}

const { as: Tag = "h1" } = Astro.props;
---

<Tag class:list={["heading", `heading-${Tag}`]}>
  <span class="heading-ink"><slot /></span>
</Tag>

<style>
  .heading {
    font-family: var(--font-heading);
    color: var(--text);
    letter-spacing: -0.02em;
    line-height: 1.25;
  }

  .heading-ink {
    display: inline;
    position: relative;
  }

  .heading-h1 {
    font-size: 2.25rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .heading-h1 .heading-ink::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 60%;
    height: 2px;
    background: var(--accent);
    opacity: 0.3;
    border-radius: 1px;
  }

  .heading-h2 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-top: 2.5rem;
    margin-bottom: 0.5rem;
  }

  .heading-h3 {
    font-size: 1.125rem;
    font-weight: 600;
    margin-top: 2rem;
    margin-bottom: 0.5rem;
  }

  @media (max-width: 600px) {
    .heading-h1 { font-size: 1.75rem; }
    .heading-h2 { font-size: 1.25rem; }
  }
</style>
